# AI Troll Lab 구현 및 배포 계획서

## 1. 프로젝트 목표

AI Troll Lab은 유튜버·스트리머가 전달받은 **GitHub Pages URL에 접속하여 별도 설치 없이 바로 플레이**할 수 있는 1인용 2D 아케이드 웹 게임이다.

플레이어는 스테이지마다 데이터 코어 3개를 수집하고, 대시·패링 반격으로 콤보를 유지하며 AI 보스의 보호막을 파괴한다. 3·6·9스테이지의 강화 선택으로 매 런의 빌드를 만들고, 시청자 투표는 핵심 진행을 대신하지 않는 보조 변수로만 작동한다. 혼자 플레이해도 판단·숙련·성장·보스 공략의 재미가 완결되는 것이 핵심이다.

## 2. 배포 및 이용 방식

- **배포 환경**: GitHub Pages 정적 웹사이트
- **접속 방식**: 공개 URL을 최신 데스크톱 브라우저에서 실행
- **지원 우선순위**: 최신 Chrome, Edge
- **입력 장치**: PC 키보드 필수
- **최소 지원 모니터**: PC 전용 1280×720, 브라우저 배율 100% 기준
- **기준 UI 해상도**: 전체 게임 페이지를 1280×720의 16:9 레이아웃으로 설계
- **일반 브라우저 실행**: 1280×720 모니터에서 주소창과 탭을 제외한 실제 영역에도 전체 UI가 들어오도록 자동 축소
- **방송 출력 기준**: OBS Browser Source 1280×720 및 1920×1080
- **큰 화면 대응**: 1280×720보다 큰 화면에서는 16:9 비율을 유지하며 가용 공간까지 확대하고 남는 영역은 중앙 정렬
- **화면 적응 공식**: `scale = min(가용 너비 / 1280, 가용 높이 / 720)`을 기준으로 전체 UI를 일괄 확대·축소
- **게임 내부 해상도**: Canvas 논리 좌표 960×600은 유지하되 CSS 표시 크기와 실제 렌더링 버퍼를 분리
- **선명도 보존**: Canvas 렌더링 버퍼에 `devicePixelRatio`를 반영해 확대 화면에서도 흐려지지 않게 처리
- **스크롤 정책**: `100vw × 100dvh` 안에서 가로·세로 페이지 스크롤 없이 표시
- **추가 화면비**: 1280×800 이상의 16:10 화면도 비율 왜곡 없이 지원
- **지원 범위 밖**: 1280×720 미만 또는 4:3 화면에서는 실행을 막지 않되 UI 가독성과 방송 품질을 보장하지 않음
- **서버 및 계정**: 회원가입·로그인 없이 플레이하며, 공개 랭킹과 비공개 건의사항만 Firebase Cloud Firestore 사용
- **웹캠**: 선택 기능이며 권한을 거부하거나 카메라가 없어도 기본 아바타로 플레이 가능
- **오디오**: 시작 버튼을 누른 뒤 Web Audio API를 초기화하며 외부 음원 파일 없이 효과음을 합성
- **모바일**: 1차 공개 범위에서 제외하고 추후 지원 여부 검토

### 기본 사용자 흐름

1. 스트리머가 전달받은 GitHub Pages URL을 연다.
2. 조작법과 선택형 웹캠 기능을 확인한다.
3. `방송 시작하기` 버튼을 눌러 게임과 오디오를 시작한다.
4. 10개 스테이지를 진행하며 AI의 방해를 파훼한다.
5. 클리어 또는 게임오버 결과를 확인하고 닉네임으로 점수를 등록하거나 즉시 재도전한다.

## 3. 핵심 게임 사양

### 조작

| 입력 | 기능 | 기본 수치 |
| --- | --- | --- |
| `A/D`, 좌우 방향키 | 좌우 이동 | 이동 속도 5 |
| `W`, 위 방향키 | 점프 | 점프력 10.5 |
| `Shift` | 대시 | 약 0.2초 지속, 약 1초 쿨타임 |
| `Space` | 패링 | 약 0.3초 지속, 약 1.5초 쿨타임 |
| `도게자` 버튼 | AI 과열 게이지 증가 | 35 증가 |

초 단위 표기는 60FPS의 기존 조작 감각을 기준으로 하며, 실제 판정 시간은 고정 시간 스텝과 delta-time으로 계산한다.

### AI 방해와 파훼

- 코어 수집과 성공한 반격은 5초 콤보를 쌓고 점수 배율을 높이며, 피격은 콤보를 초기화한다.
- 2스테이지부터 측면 충격파와 추적 낙뢰를 충분한 예고 후 교대로 사용한다.
- 5·10스테이지는 각각 2·4칸의 보스 보호막을 반격으로 파괴해야 출구가 열린다.
- 3·6·9스테이지 클리어 후 대시·패링·멘탈·콤보·자석 강화 중 3개 후보를 제시한다.
- 플레이어가 골인 큐브에 접근하면 스테이지에 따라 0.44~0.22초 동안 붉은 예고 효과를 표시한다.
- 예고 중 대시 또는 패링에 성공하면 큐브 이동을 막고 AI 과열 게이지가 증가한다.
- 대응하지 못하면 AI가 골인 큐브를 다른 유효 발판으로 이동시킨다.
- AI 과열 게이지가 100에 도달하면 스테이지에 따라 2.5~1.6초 동안 AI가 방해하지 않는다.
- 일부 상황에서 가짜 시스템 팝업과 AI 대사를 표시해 방송용 리액션을 유도한다.
- 장애물에 충돌하면 멘탈 HP가 감소하고 시작 지점으로 돌아간다.

### 스테이지

- 총 10개 스테이지로 구성한다.
- 각 스테이지는 서로 다른 발판 위의 데이터 코어 3개를 모두 수집해야 골인 큐브 잠금이 해제된다.
- 기본 발판, 움직이는 발판, 가시, 레이저와 서로 다른 골인 큐브 위치를 조합한다.
- 후반부로 갈수록 발판 폭, 이동 속도, 장애물 밀도를 높인다.
- 각 스테이지는 시작 지점에서 골인 큐브까지 실제로 도달 가능한지 수동 테스트를 통과해야 한다.

## 4. 방송용 UI 및 연출

- 스트리머 웹캠 프레임과 카메라 켜기·끄기
- 웹캠 미사용 시 멘탈 상태에 따라 표정이 변하는 기본 아바타
- 멘탈 HP, 대시 쿨타임, AI 과열, 현재 스테이지 HUD
- SOOP·치지직 실시간 방송 채팅과 로컬 테스트 입력
- AI 대사, 가짜 오류 팝업, 붉은 피격 플래시, 화면 흔들림
- 대시 잔상, 패링 충격파, 텔레포트 스파크, 클리어 파티클
- 점프, 대시, 패링, 텔레포트, 피격, 클리어용 합성 효과음
- 상단 퀵컨트롤: 명예의 전당, 후원하기, 건의사항, OBS 투명 모드, 사운드 ON/OFF 순서로 배치
- 명예의 전당·건의사항·후원 안내 창의 카카오 애드핏 728×90 광고

가짜 팝업과 가상 채팅은 실제 운영체제 오류나 실제 시청자 메시지가 아니라 게임 연출임을 UI 문맥상 구분할 수 있어야 한다.
명예의 전당·건의사항·후원 안내 창에는 카카오 애드핏 728×90을 각각 표시한다. 명예의 전당은 Firestore TOP 10을 표시하고, 건의사항은 공개 조회 없이 Firestore에 저장한다. 후원 URL은 확정 전까지 준비 중 안내를 표시한다. OBS 버튼은 게임 Canvas와 페이지 배경을 투명하게 전환한다.

## 5. 구현 상태

| 기능 | 상태 | 비고 |
| --- | --- | --- |
| 10개 스테이지 및 기본 이동·충돌 | 구현 완료 | 스테이지별 실제 클리어 여부 재검증 필요 |
| 대시·패링 | 구현 완료 | 고정 시간 스텝 기반 판정 |
| 큐브 이동 예고와 AI 과열 | 구현 완료 | 스테이지별 동적 시간 |
| 멘탈 HP와 게임오버·클리어 | 구현 완료 | 재시작 흐름 포함 |
| 방송 HUD와 2플랫폼 채팅 | 구현 완료 | SOOP·치지직 전용 Worker |
| Web Audio API 효과음 | 구현 완료 | 외부 음원 미사용 |
| 웹캠 연결과 기본 아바타 | 구현 완료 | 아바타 선택 기능은 없음 |
| 가짜 팝업·화면 흔들림·파티클 | 구현 완료 | 공개 URL에서 위치·가독성 검증 필요 |
| 결과 화면 실제 통계 집계 | 구현 완료 | 성공한 대시 파훼와 AI 과열 횟수 집계 |
| 빙판·스프링 고유 물리 효과 | 구현 완료 | 빙판 관성과 스프링 자동 점프 적용 |
| 코어 수집·콤보·점수 배율 | 구현 완료 | 3코어 출구 잠금, 5초 콤보, 피격 초기화 |
| AI 충격파·추적 낙뢰 | 구현 완료 | 예고 후 점프·대시·패링 대응 |
| 로그라이트 강화 선택 | 구현 완료 | Stage 3·6·9 뒤 3지선다 |
| 중간·최종 보스 보호막 | 구현 완료 | Stage 5는 2칸, Stage 10은 4칸 |
| 1280×720 반응형 방송 레이아웃 | 구현 완료 | 1280×720 기준 비율 확대·축소 및 중앙 정렬 |
| 고해상도 Canvas 선명도 | 구현 완료 | CSS 크기와 DPR·화면 배율 기반 렌더링 버퍼 분리 |
| delta-time 기반 타이머 | 구현 완료 | 고정 시간 스텝으로 판정 일관성 확보 |
| 건의사항 메뉴 | 구현 완료 | Firestore 비공개 등록, 공개 읽기 차단 |
| 상단 방송 퀵컨트롤 | 구현 완료 | 명예의 전당·후원·건의사항·OBS·사운드 순서로 배치 |
| 명예의 전당 | 구현 완료 | Firestore 점수 등록·공개 TOP 10 조회 및 방송 연동 상태 표시 |
| 시작 전 방송 연동 | 구현 완료 | 시작 화면과 채팅 패널에서 SOOP·치지직 채널 등록 가능 |
| 카카오 애드핏 광고 | 구현 완료 | 명예의 전당·후원하기·건의사항 728×90 단위 연결, 로드 실패 시 안전한 자리 표시 |
| OBS 투명 모드 | 구현 완료 | 페이지와 Canvas 배경 투명 전환 |
| 후원 메뉴 | 연결 코드 완료 | 후원 URL 변수 미등록 시 준비 안내 표시 |
| 자동 테스트 | 구현 완료 | 밸런스·점수·10개 스테이지·투표·Worker 검사 |
| Worker 소스 관리 | 구현 완료 | Origin·호스트·본문 크기·리다이렉트 제한 |
| 바닥 워프 포탈 | 후속 검토 | 1차 공개 범위에 포함하지 않음 |
| 중력 왜곡 | 후속 검토 | 상태값만 존재하며 현재 게임에서 발동하지 않음 |
| CRT 픽셀화·슬로 모션 | 후속 검토 | 현재 미구현, 안정화 후 도입 검토 |
| 다중 아바타 선택 | 후속 검토 | 현재 기본 아바타 한 종류만 제공 |

## 6. 기술 구성

- **게임 엔진**: HTML5 Canvas 2D, JavaScript ES Modules
- **UI**: HTML5, Vanilla CSS
- **오디오**: Web Audio API procedural synthesis
- **개발 및 빌드**: Vite 5, Node.js
- **데이터**: Firebase Cloud Firestore, Firebase Web SDK Lite
- **배포 결과물**: `npm run build`로 생성되는 `dist/` 정적 파일
- **경로 정책**: GitHub Pages 저장소 하위 경로에서 동작하도록 Vite 상대 경로 사용

```text
src/
├── main.js                 # 초기화, 시작·재시작·웹캠 UI 이벤트
├── style.css               # 방송 레이아웃과 시각 효과
├── game/
│   ├── Engine.js           # 게임 루프, 충돌, 진행 및 결과
│   ├── Player.js           # 이동, 대시, 패링
│   ├── AIBully.js          # 텔레그래프, 큐브 이동, 과열
│   ├── AIAttackDirector.js # 예고형 충격파·추적 낙뢰 패턴
│   ├── RunProgression.js   # 코어, 콤보, 보스 보호막, 강화
│   ├── Stage.js            # 10개 스테이지와 장애물
│   └── Particle.js         # 파티클 연출
├── firebase/
│   ├── firebaseConfig.js   # 빌드 환경 기반 Firebase 초기화
│   ├── FirestoreService.js # 랭킹·건의사항 데이터 접근
│   └── CommunityController.js # 등록·조회 UI
└── ui/
    ├── BroadcastHUD.js     # HUD, 가상 채팅, 팝업
    └── AudioEngine.js      # 합성 효과음
```

## 7. 공개 전 운영 체크리스트

### 코드 및 자동 검증 완료

- [x] 전체 페이지의 기준 UI를 1280×720 16:9 레이아웃으로 재구성한다.
- [x] `scale = min(가용 너비 / 1280, 가용 높이 / 720)` 방식으로 전체 UI를 확대·축소하고 중앙 정렬한다.
- [x] 1280×720 기준 영역 안에서 전체 UI가 일괄 축소되도록 구성한다.
- [x] 브라우저 기본 배율 100%에서 페이지 스크롤이 생기지 않도록 구성한다.
- [x] 960×600 Canvas의 논리 좌표와 입력 좌표를 화면 확대·축소 후에도 일치시킨다.
- [x] `devicePixelRatio`를 반영해 1920×1080 등 큰 화면에서도 Canvas가 흐려지지 않게 한다.
- [x] 고정 시간 스텝 기반 타이머로 프레임 속도와 무관한 판정을 유지한다.
- [x] 결과 화면의 대시 성공 횟수와 AI 과열 횟수를 실제 플레이 데이터로 집계한다.
- [x] 자동 테스트로 10개 스테이지의 발판·시작점·골인 위치 범위를 검사한다.
- [x] 빙판과 스프링의 고유 물리를 구현한다.
- [x] 재시작 시 기존 애니메이션 루프를 정리한다.
- [x] 게임 중 방향키와 Space의 브라우저 기본 동작을 차단한다.
- [x] 웹캠 권한 거부 시 기본 아바타로 복구하고 종료 시 카메라 트랙을 정리한다.
- [x] 웹캠 영상은 브라우저 내부에서만 사용하고 외부 서버로 전송하지 않음을 안내한다.
- [x] 애드핏 단위가 없거나 로드에 실패해도 게임에 영향 없는 자리 표시를 제공한다.
- [x] 후원 URL이 없으면 준비 상태를 표시하고, 변수 등록 시 자동 연결한다.
- [x] 탭 전환 시 프레임 누적값과 입력 상태를 초기화한다.

### 실제 장비·외부 계정 확인

- [ ] 방송 중인 SOOP 채널에서 실시간 메시지 수신 확인
- [ ] 방송 중인 치지직 채널에서 실시간 메시지 수신 확인
- [ ] Chrome·Edge에서 10스테이지 수동 완주
- [ ] OBS Browser Source 1280×720·1920×1080 육안 확인
- [x] 애드핏 매체 승인 및 실제 광고 단위 연결
- [ ] 실제 후원 URL 결정 및 변수 등록
- [x] 공개 GitHub Pages URL에서 HTML, CSS, JavaScript 및 Firebase 랭킹 조회 확인

### 후속 안정성 및 방송 품질

- 1280×800, 1366×768 및 1920×1080 화면에서 여백과 UI 가독성을 조정한다.
- 가짜 팝업이 조작 안내나 핵심 HUD를 영구적으로 가리지 않도록 제한한다.
- 음소거와 웹캠 등 사용자 선택을 재시작 시 일관되게 유지한다.
- 외부 Google Fonts 로딩에 실패해도 시스템 대체 글꼴로 게임을 정상 실행한다.

### P2 — 후속 콘텐츠

- 워프 포탈, 중력 왜곡, CRT 픽셀화, 슬로 모션을 안정성을 해치지 않는 범위에서 추가한다.
- 아바타 선택과 방송용 사용자 설정을 검토한다.
- AI 대사·가상 채팅 문구를 플레이 테스트 결과에 맞춰 조정한다.

## 8. 테스트 및 완료 기준

1차 공개 버전은 다음 조건을 모두 만족하면 완료로 판단한다.

- 스트리머가 GitHub Pages URL을 열어 별도 설치와 로그인 없이 게임을 시작할 수 있다.
- 웹캠 권한을 거부하거나 카메라가 없어도 모든 핵심 게임 기능을 사용할 수 있다.
- 최신 Chrome과 Edge에서 게임오버 또는 10스테이지 클리어 후 재시작할 수 있다.
- 공개 URL에서 콘솔의 치명적 오류와 정적 파일 404가 발생하지 않는다.
- 키 입력, 게임 루프, 오디오, 웹캠이 재시작 과정에서 중복되거나 누적되지 않는다.
- 결과 화면에 실제 플레이 결과가 표시된다.
- 10개 스테이지 모두 정상적인 입력으로 도달 및 클리어할 수 있다.
- 1280×720 모니터의 최대화된 Chrome과 Edge에서 주소창을 포함한 일반 브라우저 상태로 플레이할 수 있다.
- 1280×720 OBS Browser Source에서 캔버스, HUD, 웹캠, 채팅 및 주요 버튼이 겹치거나 잘리지 않는다.
- 1280×800과 1366×768 화면에서도 불필요한 스크롤이나 비율 왜곡이 발생하지 않는다.
- 1920×1080 화면에서 전체 UI가 16:9 비율로 확대되고 Canvas가 흐려지지 않는다.
- 화면 확대·축소 후에도 DOM 버튼을 정상적으로 클릭할 수 있고 Canvas의 렌더링 위치와 충돌 좌표가 일치한다.
- 프레임 속도가 달라져도 텔레그래프, 대시, 패링 및 과열 시간이 설계값과 일치한다.

## 9. 배포 절차

1. `npm install`로 의존성을 설치한다.
2. `npm run build`를 실행해 빌드 오류가 없는지 확인한다.
3. `npm run preview`에서 프로덕션 결과물을 로컬 점검한다.
4. 검증된 변경 사항을 `main` 브랜치에 반영한다.
5. `.github/workflows/deploy.yml`의 GitHub Actions가 `dist/`를 GitHub Pages에 자동 배포한다.
6. Actions 배포 성공 여부와 생성된 Pages URL을 확인한다.
7. 실제 공개 URL에서 P0 체크리스트와 완료 기준을 다시 검증한다.
8. 검증된 URL만 스트리머에게 전달한다.

## 10. 1차 공개 범위에서 제외되는 항목

- YouTube·Twitch 채팅 연동(API 할당량 보호를 위해 SOOP·치지직만 지원)
- 회원 계정, 로그인, 개인 저장 데이터
- 모바일 터치 조작과 게임패드 지원
- 방송 송출 프로그램 직접 연동
- 바닥 워프 포탈, 중력 왜곡, CRT 픽셀화, 슬로 모션
- 다중 아바타 선택

제외 항목은 URL 배포 안정화와 핵심 게임 플레이 검증이 끝난 뒤 별도 우선순위로 관리한다.
